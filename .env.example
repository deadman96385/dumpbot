# Telegram Bot Token from BotFather
TELEGRAM_BOT_TOKEN=""

# Jenkins Server Details
JENKINS_URL="http://127.0.0.1:8080" # Your Jenkins server URL
JENKINS_USER_NAME=""                # Jenkins username with job build/cancel permissions
JENKINS_USER_TOKEN=""               # Jenkins API Token for the user above

# Telegram Chat IDs (Use @userinfobot or similar to get these)
# The chat where users send #request messages
REQUEST_CHAT_ID=""
# The chat where admins/reviewers see requests and use commands like /accept, /reject, /dump, /cancel
REVIEW_CHAT_ID=""